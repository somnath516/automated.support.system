# TODO

- [ ] Fix settings.html: robust username/email input binding (use explicit IDs; remove index-based `.card input` selection).
- [ ] Fix settings.html: standardize `localStorage.darkMode` value to match adminDashboard.html ("enabled" vs boolean).
- [ ] Fix settings.html: make logout navigation path absolute (`/role.html`).
- [ ] Fix settings.html: guard dark toggle element + validate `/api/settings` response before using `data.username`/`data.email`.

